// Initialize the array p
let p = [];

// Assign a value x to an index i
let i = 2; // Example index, can be any valid array index
let x = 10; // Example value to assign
p[i] = x;

// Now p will be [empty, empty, 10] assuming no previous elements were defined
console.log(p); // Output: [empty, empty, 10]
