// Save the end value in a variable
let initialValueForNextAnimation = endValueOfPreviousAnimation;

// Use the variable as the initial value for the next animation
animateElement(initialValueForNextAnimation, targetValueForNextAnimation);
